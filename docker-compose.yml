
services:
  photobooth:
    build: .
    privileged: true  # required for USB device access
    group_add:
      - "0"  # let the non-root "application" user access root:root-owned /dev/bus/usb nodes
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./data/images:/app/data/images
